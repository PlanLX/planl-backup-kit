# PlanLX Elasticsearch Snapshot Kit - 环境变量配置示例
# 复制此文件为 .env 并根据您的环境修改相应的值

# =============================================================================
# Elasticsearch 源集群配置 (快照时使用)
# =============================================================================

# 快照 Elasticsearch 集群地址 (多个地址用逗号分隔)
SNAPSHOT_HOSTS=http://localhost:9200,http://localhost:9201

# 快照集群认证 (可选)
SNAPSHOT_USERNAME=elastic
SNAPSHOT_PASSWORD=your-password

# 快照集群 SSL 证书验证 (true/false)
SNAPSHOT_VERIFY_CERTS=true

# =============================================================================
# Elasticsearch 恢复集群配置 (恢复时使用)
# =============================================================================

# 恢复 Elasticsearch 集群地址 (多个地址用逗号分隔)
RESTORE_HOSTS=http://localhost:9200,http://localhost:9201

# 恢复集群认证 (可选)
RESTORE_USERNAME=elastic
RESTORE_PASSWORD=your-password

# 恢复集群 SSL 证书验证 (true/false)
RESTORE_VERIFY_CERTS=true

# =============================================================================
# Elasticsearch 快照配置
# =============================================================================

# S3 存储库名称
ES_REPOSITORY_NAME=my-s3-repository

# 快照名称 (可选，如果不设置将自动生成)
ES_SNAPSHOT_NAME=

# 要快照/恢复的索引 (多个索引用逗号分隔)
ES_INDICES=my-index-1,my-index-2,logs-*

# =============================================================================
# AWS S3 配置
# =============================================================================

# S3 存储桶名称
S3_BUCKET_NAME=my-elasticsearch-snapshots

# S3 基础路径 (快照在 S3 中的存储路径)
S3_BASE_PATH=elasticsearch-snapshots

# AWS 区域
S3_REGION=us-east-1

# =============================================================================
# AWS 凭证配置
# =============================================================================

# AWS 访问密钥 ID
AWS_ACCESS_KEY_ID=your-access-key-id

# AWS 秘密访问密钥
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# AWS 区域 (如果与 S3_REGION 不同)
AWS_REGION=us-east-1

# =============================================================================
# 操作配置
# =============================================================================

# 操作超时时间 (秒)
TIMEOUT=300

# 是否等待操作完成 (true/false)
WAIT_FOR_COMPLETION=true

# =============================================================================
# 使用说明
# =============================================================================
# 
# 1. 复制此文件为 .env:
#    cp env.example .env
#
# 2. 编辑 .env 文件，填入您的实际配置值
#
# 3. 运行命令时使用 --config .env 参数:
#    uv run python main.py --config .env snapshot
#
# 4. 或者直接设置环境变量:
#    export SNAPSHOT_HOSTS=http://your-es-cluster:9200
#    export AWS_ACCESS_KEY_ID=your-key
#    uv run python main.py snapshot
#
# 注意: 敏感信息如密码和密钥建议使用环境变量而不是配置文件 